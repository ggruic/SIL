# Sudreg Plugin Architecture Notes

This package is an example SIL Plugin for the Sudreg application.

The package demonstrates the non-Pipeline MVP scenario:

- Explain Company

The plugin owns the `sudreg.*` namespace.

All examples are architecture-level examples and should be adapted to the actual Sudreg API.
