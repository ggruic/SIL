# Sudreg Approvals

The MVP `sudreg.company.explain` flow is read-only and does not require approval by default.

If future Sudreg flows perform modifications or high-impact operations, approval definitions should be added here.
